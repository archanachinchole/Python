#Write a function to find the largest element in a list


def find_largest(arr):
    largest = arr[0]
    for num in arr:
        if num > largest:
            largest = num
    return largest

print(find_largest([1, 5, 8, 12, 3]))  # Output: 12
