def calculate_median(numbers):
    numbers.sort()
    n = len(numbers)
    mid = n // 2
    return (numbers[mid] + numbers[-mid-1]) / 2 if n % 2 == 0 else numbers[mid]

print(calculate_median([5, 1, 3, 2, 4]))  # Output: 3
